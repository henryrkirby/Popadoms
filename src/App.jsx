/**
 * App.jsx — Root component for the Popadoms app.
 * Minimal wrapper — the game takes up the entire screen.
 */

import Game from "./components/Game.jsx";

function App() {
  return <Game />;
}

export default App;
